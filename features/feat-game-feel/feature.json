{
  "featureKey": "feat-2026-048",
  "title": "Game Feel — Haptics, Animations & Visual Feedback",
  "status": "frozen",
  "problem": "Combat, level-ups, and loot drops are all text-only events with no physical or visual feedback. Hits, crits, defeats, and victories all feel the same. There are no screen shake animations, no HP bar flash on damage taken, no haptic pulses, no level-up overlay. The gap between mechanics and feel makes the game feel like a spreadsheet rather than an action RPG.",
  "schemaVersion": 1,
  "domain": "game/ux",
  "analysis": "CombatScreen uses static Text/View components with no animation. React Native Animated API is available with no new packages. expo-haptics is included in Expo SDK and just needs an import. Combat log is a plain list of grey strings. Level-up is only visible in the combat log text. GridScreen tile taps have no feedback. The gap between a crit doing 80 damage and a normal hit doing 10 is invisible — both look identical in the UI.",
  "implementation": "1. src/hooks/useHaptics.ts — wraps expo-haptics: impactLight, impactMedium, impactHeavy, notificationSuccess, notificationError. Guards against haptics disabled in settingsStore. 2. src/hooks/useCombatAnimations.ts — returns Animated.Value instances: shakeX (translateX for player HP bar), monsterFlash (opacity/color for monster HP bar), levelUpPulse (scale for level-up overlay). triggerShake(): Animated.sequence([...3 oscillations...]).start(). triggerFlash(): Animated sequence on monster bar. 3. CombatScreen.tsx — wrap player HP bar in Animated.View with translateX from shakeX. Wrap monster HP bar section in Animated.View with opacity flash. In attackAction callback: trigger monsterFlash. When playerHp decreases: trigger shakeX + impactMedium. On crit in log: impactHeavy. On victory: notificationSuccess. On defeat: notificationError. On potion: impactLight. 4. src/components/LevelUpOverlay.tsx — full-screen semi-transparent gold overlay. Shows '★ LEVEL UP ★', new level number, stat gains. useEffect watches gameStore.level — when it increases, animate in (scale 0→1, opacity 0→1) then auto-dismiss after 1.8s. 5. Combat log color coding: lines containing 'crit'/'CRIT' → #f39c12 orange, 'Victory'/'XP' → #2ecc71 green, 'damage'/'dealt' → #e74c3c red, 'healed'/'Potion'→ #27ae60, 'Scroll'→ #9b59b6, 'LEVEL UP'→ #c8a020 gold, default → #666 grey. 6. GridScreen.tsx — tile onPress: impactLight. Encounter discovery (boss/rare/chest): impactHeavy. 7. All haptic calls gated by settingsStore.hapticsEnabled (read from store directly, no prop drilling).",
  "decisions": [
    {
      "decision": "Haptics via expo-haptics with silent fallback for devices without support",
      "rationale": "expo-haptics is already in the Expo SDK; the try/catch silent fallback pattern means haptics degrade gracefully on simulators and older devices.",
      "date": "2026-03-29"
    },
    {
      "decision": "Log entry types drive color — crit gold, xp teal, scroll purple, victory green, levelUp gold, heal green, enraged orange",
      "rationale": "Typed log entries decouple color from content string parsing; adding a new event type only requires adding to the union.",
      "date": "2026-03-29"
    },
    {
      "decision": "BMAD S3: tier background tints — TIER_BG map with 5 subtle hues per tier applied to gridArea background",
      "rationale": "Subtle tints signal depth tier change without being distracting; pure black for all tiers made every tier feel identical.",
      "date": "2026-04-01"
    },
    {
      "decision": "BMAD S3: per-item quality loot log in handleVictory — star unique, diamond rare, diamond magic, dot common — each with typed log color",
      "rationale": "Previously all loot dropped as a single summary line. Per-item lines with quality prefixes let players see at a glance what they found without leaving the combat log.",
      "date": "2026-04-01"
    },
    {
      "decision": "BMAD S3: flee button label changed to stays/blocked to clarify that fleeing keeps the player on the current floor",
      "rationale": "Players assumed flee meant game over or retreat to town. The label stays makes clear the cost is staying on the floor, not losing progress.",
      "date": "2026-04-01"
    },
    {
      "decision": "Kill-shot flash (red) and crit flash (gold) implemented as separate full-screen Animated.View overlays rather than animating the individual bar",
      "rationale": "Screen-level flashes read better on small mobile screens than localized bar flashes; two separate overlays keep the animations decoupled and independently tunable.",
      "date": "2026-04-03"
    },
    {
      "decision": "Victory/defeat card uses spring entrance (scale 0.82→1) rather than a slide or fade alone",
      "rationale": "Spring gives a physical 'landed' feel that matches the weight of the combat outcome. Pure fades felt flat; slides felt like a menu transition.",
      "date": "2026-04-03"
    },
    {
      "decision": "Loot items stagger-slide in per-item (55ms delay per index) rather than all at once",
      "rationale": "Simultaneous entrance makes drops feel like a list dump. Staggering creates a slot-machine reveal rhythm that makes even two-item drops feel curated.",
      "date": "2026-04-03"
    },
    {
      "decision": "Descend button color matches selected class color rather than always red",
      "rationale": "Red had no semantic relationship to the action — it was borrowed from the game's danger/blood palette. Class color creates identity and reinforces the class selection moment.",
      "date": "2026-04-03"
    },
    {
      "decision": "Low-HP pulse on HUD orb pulses the entire orb opacity (0.35–1.0) rather than just the border",
      "rationale": "Pulsing the whole orb is dramatically more visible at small (46px) sizes than a 2px border change. The border color also shifts to red for additional signal.",
      "date": "2026-04-03"
    }
  ],
  "successCriteria": "Combat hit triggers haptic. Player HP bar visibly shakes on damage received. Monster HP bar flashes red when damaged. Kill-shot triggers a red screen flash before VICTORY appears. Crit hits trigger a brief gold screen flash. Victory/defeat card punches in with spring scale animation. Level-up overlay appears and dismisses after 1.8s. Combat log lines have distinct colors by event type. Loot items slide in staggered on LootScreen mount. Boss/Ancient loot screen shows a colored vignette border and scale-punch header. HP orb pulses red when HP < 25%. Descend button uses class color. Pickup haptic on item grab.",
  "tags": [
    "haptics",
    "animations",
    "game-feel",
    "ux",
    "combat"
  ],
  "knownLimitations": [
    "No particle effects (would require a dedicated library like react-native-skia)",
    "Shake animation is horizontal only — vertical shake not implemented",
    "LevelUpOverlay only triggers mid-combat — level-up during town/menu transitions won't show it (rare edge case)"
  ],
  "componentFile": "src/hooks/useHaptics.ts, src/hooks/useCombatAnimations.ts, src/screens/CombatScreen.tsx, src/components/LevelUpOverlay.tsx, src/screens/GridScreen.tsx, src/components/HUD.tsx, src/screens/LootScreen.tsx, src/screens/ClassSelectScreen.tsx",
  "userGuide": "Game feel is transparent to the player — you just feel it. Hits shake the screen. Crits hit harder. Level-ups are celebrated. If haptics feel too intense, toggle them off in Settings.",
  "lineage": {
    "parent": "feat-2026-034",
    "children": [
      "feat-2026-071"
    ]
  },
  "lastVerifiedDate": "2026-04-03",
  "revisions": [
    {
      "date": "2026-03-29",
      "author": "claude",
      "fields_changed": [
        "analysis",
        "implementation",
        "decisions",
        "successCriteria"
      ],
      "reason": "Initial full feature definition for game feel"
    },
    {
      "date": "2026-04-01",
      "author": "claude+marc",
      "fields_changed": [
        "decisions"
      ],
      "reason": "BMAD session 3 — tier tints, loot log quality prefixes, affix names, flee label"
    },
    {
      "date": "2026-04-03",
      "author": "claude+marc",
      "fields_changed": [
        "decisions",
        "successCriteria"
      ],
      "reason": "BMAD S7: kill-shot flash, crit flash, victory spring entrance, flee haptics, low-HP orb pulse, loot stagger slide-in, boss loot vignette+scale punch, pickup haptic, descend button class-colored"
    }
  ],
  "annotations": [
    {
      "id": "reopen-1775065876422",
      "author": "lac advance",
      "date": "2026-04-01",
      "type": "reopen",
      "body": "BMAD session 3: tier background tints on gridArea (5 subtle hues per tier), per-item quality loot log with color-coded prefixes, monster affix names in combat-start log, flee label \"stays\"/\"blocked\""
    }
  ],
  "npmPackages": [
    "expo-haptics"
  ],
  "externalDependencies": [],
  "publicInterface": [
    {
      "name": "useHaptics",
      "type": "() => { impactLight, impactMedium, impactHeavy, notificationSuccess, notificationError }",
      "description": "Haptic feedback hook — all calls gated by settingsStore.hapticsEnabled"
    },
    {
      "name": "useCombatAnimations",
      "type": "() => { shakeX, monsterFlash, triggerShake, triggerFlash, levelUpScale }",
      "description": "Animated values and trigger functions for combat visual feedback"
    },
    {
      "name": "LevelUpOverlay",
      "type": "React.FC",
      "description": "Full-screen gold overlay shown when gameStore.level increases. Auto-dismisses after 1.8s"
    }
  ],
  "codeSnippets": [
    {
      "label": "useHaptics hook",
      "snippet": "import * as Haptics from 'expo-haptics'\nimport { useSettingsStore } from '../store/settingsStore'\n\nexport function useHaptics() {\n  const enabled = useSettingsStore(s => s.hapticsEnabled)\n  return {\n    impactLight:          () => enabled && Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light),\n    impactHeavy:          () => enabled && Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Heavy),\n    notificationSuccess:  () => enabled && Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success),\n    notificationError:    () => enabled && Haptics.notificationAsync(Haptics.NotificationFeedbackType.Error),\n  }\n}"
    },
    {
      "label": "Player HP bar shake animation",
      "snippet": "const shakeX = useRef(new Animated.Value(0)).current\nfunction triggerShake() {\n  Animated.sequence([\n    Animated.timing(shakeX, { toValue: -8, duration: 50, useNativeDriver: true }),\n    Animated.timing(shakeX, { toValue:  8, duration: 50, useNativeDriver: true }),\n    Animated.timing(shakeX, { toValue: -4, duration: 40, useNativeDriver: true }),\n    Animated.timing(shakeX, { toValue:  0, duration: 40, useNativeDriver: true }),\n  ]).start()\n}"
    }
  ],
  "toolingAnnotations": [],
  "statusHistory": [
    {
      "from": "draft",
      "to": "active",
      "date": "2026-03-29"
    },
    {
      "from": "active",
      "to": "frozen",
      "date": "2026-03-29"
    },
    {
      "from": "frozen",
      "to": "active",
      "date": "2026-04-01",
      "reason": "BMAD session 3: tier background tints on gridArea (5 subtle hues per tier), per-item quality loot log with color-coded prefixes, monster affix names in combat-start log, flee label \"stays\"/\"blocked\""
    },
    {
      "from": "active",
      "to": "frozen",
      "date": "2026-04-01"
    }
  ],
  "implementationNotes": [
    "All screen-flash overlays (kill-shot red, crit gold) use StyleSheet.absoluteFillObject + pointerEvents=none + useNativeDriver:true — they sit at the top of the component tree but never intercept touches.",
    "GlobeOrb is a pure function component that now owns its own pulse animation via useEffect([lowHp]) — the animation starts/stops based on the prop, so no animation state leaks when HP recovers above 25%.",
    "AnimatedLootRow uses a single-fire Animated.parallel on mount (not a loop), so no cleanup return is needed — the animation self-terminates and frees memory.",
    "handleFlee reads useCombatStore.getState().outcome synchronously after calling fleeAction() because Zustand state updates are synchronous — no setTimeout or promise needed to detect flee success/failure.",
    "The boss loot vignette is a borderWidth overlay (not a shadow) so it works consistently on both iOS and Android without platform-specific shadow props."
  ]
}
